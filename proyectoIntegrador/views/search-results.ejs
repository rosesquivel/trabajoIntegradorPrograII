<!DOCTYPE html>
<html lang="es">
	<!-- Include Head -->
	<%-include('./partials/head')%>

	<body>
		<!-- Include Header -->
		<% if (locals.user != undefined) { %>
			<%-include('./partials/headerLogueado')%> 
		<% } else { %>
			<%-include('./partials/header')%>
		<%}%>

		<main>
			<!-- Novedades -->
			<div class="container products-wrapper">
				<div class="row">
					<div class="col-12">
						<h2 class="products-title">Resultados de búsqueda para: <%= results %></h2>
					</div>
				
					<!-- Show mis productos -->
					<% if (products !='' ) {%>
					
					<% for(let i=0; i<products.length; i++){ %>
						<div class="col-12 col-sm-6 col-lg-3">
							<section class="product-box">
								<a href="/products/id/<%= products[i].id %>">
									<figure class="product-box_image">
										<img src=<%=products[i].image%> alt="<%=products[i].name%>">
									</figure>
									<article class="product-box_data">
										<h2><%=products[i].name%></h2>
										<img src="<%=products[i].user.profilePicture%>" alt="">
										<p><%=products[i].user.username%></p>
										<p><%=products[i].shortDescription%></p>
										<p>Comentarios: <%=products[i].comments.length%></p>
									</article>
								</a>
							</section>
						</div>
					<% } %>
				<% }else {%>
					<h2>No hay resultados para su criterio de búsqueda</h2> 
				<%}%>  <!-- HACER UNA PAGINA COMO LA DE ERROR 404  -->
				</div>
			</div>
		</main>

		<!-- Include Footer -->
		<%-include('./partials/footer')%>
	</body>
</html>